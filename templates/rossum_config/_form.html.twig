{{ form_start(form) }}
    <div class="ibox">
        <div class="ibox-content col-lg-8 col-lg-offset-2">
            <div class="row">
            	<div class="">
	                <div class="col-lg-12 form-group lg-offset-3">
	                    {{ form_label(form.mkey, 'Nom du dossier') }}
	                    {{ form_row(form.mkey) }}
	                </div>
	                <div class="col-lg-12 form-group">
	                    {{ form_label(form.value, 'id du dossier') }}
	                    {{ form_row(form.value) }}
	                </div>
	                <div class="col-lg-12 form-group">
	                	<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
	                </div>
	            </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}
