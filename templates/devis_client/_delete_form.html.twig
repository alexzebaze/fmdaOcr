<form method="post" action="{{ path('devis_pro_delete', {'id': devis_pro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devis_pro.id) }}">
    <button class="btn">Delete</button>
</form>
