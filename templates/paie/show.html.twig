{% extends 'base.html.twig' %}

{% block title %} {{parent()}} Paie{% endblock %}

{% block body %}
    <h1>Paie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paie.id }}</td>
            </tr>
            <tr>
                <th>Heure_sup</th>
                <td>{{ paie.heureSup }}</td>
            </tr>
            <tr>
                <th>Panier</th>
                <td>{{ paie.panier }}</td>
            </tr>
            <tr>
                <th>Trajet</th>
                <td>{{ paie.trajet }}</td>
            </tr>
            <tr>
                <th>Cout_global</th>
                <td>{{ paie.coutGlobal }}</td>
            </tr>
            <tr>
                <th>Salaire_net</th>
                <td>{{ paie.salaireNet }}</td>
            </tr>
            <tr>
                <th>Conges_paye</th>
                <td>{{ paie.congesPaye }}</td>
            </tr>
            <tr>
                <th>Document_file</th>
                <td>{{ paie.documentFile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paie_index') }}">back to list</a>

    <a href="{{ path('paie_edit', {'id': paie.id}) }}">edit</a>

    {{ include('paie/_delete_form.html.twig') }}
{% endblock %}
