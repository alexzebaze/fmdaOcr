{{ form_start(form) }}
<div class="row">
    <div class="col-lg-2 form-group">
        {{ form_row(form.mois) }}
    </div>
    <div class="col-lg-2 form-group">
        {{ form_row(form.mois2) }}
    </div>
    <div class="col-lg-2 form-group">
        {{ form_row(form.annee) }}
    </div>
    <div class="col-lg-2 form-group">
        <button class="btn btn-primary" style="margin-top: 21px">{{ button_label|default('Appliquer') }}</button>
    </div>
    <input type="hidden" name="tab" value="taches-tab">
</div>
{{ form_end(form) }}
