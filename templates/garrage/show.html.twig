{% extends 'base.html.twig' %}

{% block title %}Garrage{% endblock %}

{% block body %}
    <h1>Garrage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garrage.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ garrage.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('garrage_index') }}">back to list</a>

    <a href="{{ path('garrage_edit', {'id': garrage.id}) }}">edit</a>

    {{ include('garrage/_delete_form.html.twig') }}
{% endblock %}
