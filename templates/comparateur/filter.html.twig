{{ form_start(form) }}
<div class="row">
    <div class="col-lg-3 form-group">
        {{ form_row(form.mois) }}
    </div>
    <div class="col-lg-3 form-group">
        {{ form_row(form.annee) }}
    </div>
    <div class="col-lg-3 form-group">
        {{ form_row(form.fournisseur) }}
    </div>

    <div class="col-lg-3 form-group">
        <button class="btn btn-primary btnAppliqueFilter" style="margin-top: 21px">{{ button_label|default('Appliquer') }}</button>
        {#<a class="btn btn-primary" style="margin-top: 21px;margin-left: 5px;" href="{{path('bon_livraison_list')}}">tout</a>#}
    </div>
</div>
{{ form_end(form) }}
