{% extends 'base.html.twig' %}

{% block title %} {{parent()}} PrevisionelCategorie{% endblock %}

{% block body %}
    <h1>PrevisionelCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ previsionel_categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ previsionel_categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('previsionel_categorie_index') }}">back to list</a>

    <a href="{{ path('previsionel_categorie_edit', {'id': previsionel_categorie.id}) }}">edit</a>

    {{ include('previsionel_categorie/_delete_form.html.twig') }}
{% endblock %}
